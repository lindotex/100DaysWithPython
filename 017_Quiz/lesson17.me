# Class creation


Notation:

class className():
    # class atributes 
 
    # class methods

class User(): # Classes in Python should aways be written in PascalCase.
    pass # you can use the magic word 'pass', if you don't want to create something at the monent. But still wanna the program running.

    def __init__(self, user_id, name, age):
        self.id = user_id
        self.name = name
        self.age = age

